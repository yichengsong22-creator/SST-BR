dataset:
  rhb_root: "/path/to/RHB_train"

sampling:
  radar_hz: 120.0
  ppg_hz: 30.0

windowing:
  source_segment_seconds: 30.0
  local_window_seconds: 5.0
  evaluation_window_seconds: 10.0

demo:
  random_seed: 2026
  note: "Educational structural demo; not the reported-result implementation."

runtime:
  log_level: INFO
