# Synthetic Binder data

`generate_demo_data.py` is a lightweight example wrapper around the installable
`sstbr.demo.synthetic` helper. It creates all demonstration arrays in memory
with a fixed random seed. The signals are educational and do not represent an
RHB subject, manuscript result, private feature cache, or trained model.

No generated binary fixture is committed to the repository.
