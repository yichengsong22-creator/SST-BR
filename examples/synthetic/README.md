# Synthetic Binder data

`generate_demo_data.py` creates all demonstration arrays in memory with a fixed
random seed. The signals are educational and do not represent an RHB subject,
paper prediction, private feature cache, or trained model.

No generated binary fixture is committed to the repository.
